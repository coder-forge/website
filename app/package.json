{
  "name": "coder-forge",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "test": "meteor test --once --driver-package dispatch:mocha-phantomjs"
  },
  "dependencies": {
    "bootstrap": "^3.3.6",
    "meteor-node-stubs": "^0.2.3"
  }
}
